{
  "name": "xola-mods",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "clipboard": "^2.0.0",
    "faker": "^4.1.0",
    "jquery": "^3.2.1",
    "lodash": "^4.17.4",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.17",
    "spectre.css": "^0.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "rm build/service-worker.js && rm build/asset-manifest.json",
    "build-watch": "nodemon node_modules/.bin/react-scripts build",
    "build-zip": "yarn build && rm -f xola-impersonator.zip && zip -r xola-impersonator.zip build/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "nodemon": "^1.14.11"
  }
}
